//function that generates an error page when inputing a non existent route path.
function ErrorPage() {
    return (
      <div className="error-page">
        <h1 className="error-text">DOES NOT COMPUTE</h1>
      </div>
    );
  }
  
  export default ErrorPage;
  